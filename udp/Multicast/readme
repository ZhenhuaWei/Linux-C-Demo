1. gcc multi_client.c -o multi_client
2. gcc multi_server_1.c -o multi_server_1 这个不是通常说的服务器，而是加入组播的ip的一个测试demo，该程序可以运行在不同的电脑终端上。
3. 注意：组播的概念与单播、广播有比较大的区别，例如组播的ip地址段需要是：224.0.1.0～238.255.255.255之间，与本机的ip无关